# Copy this file to .env.local and set the value to your Codespace name
# (the part before "-8000.app.github.dev" in your forwarded port URL).
# Without it, API requests fall back to http://localhost:8000.
VITE_CODESPACE_NAME=
